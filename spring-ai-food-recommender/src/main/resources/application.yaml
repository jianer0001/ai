spring:
  application:
    name: spring-ai-food-recommender
  
  # 数据库配置（请根据实际情况修改）
  datasource:
    url: jdbc:mysql://192.168.10.215:3306/jeepay?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: HXN123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  ai:
    openai:
      api-key: ms-5791fb33-3531-4d84-937c-1e6348b046bd
      base-url: https://ms-ens-08214f56-2b9d.api-inference.modelscope.cn/v1
      chat:
        options:
          model: Qwen/Qwen3-4B
          temperature: 0.7

# 服务器配置
server:
  port: 8080

# 日志配置
logging:
  level:
    com.example.foodai: DEBUG
    org.springframework.ai: DEBUG
